#include "main.h"

/**
 * swap_int - Swaps the values of two integers
 * @a: pointer to first integer
 * @b: pointer to second integer
 *
 * Return: void
 */

int _strlen(char *s)
{
        int i;

        i = 0;
	    while (s[i] != '\0')
	    {
	    	i++;
	    }
	    return (i);
}
