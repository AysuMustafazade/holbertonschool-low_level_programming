#include "main.h"

/**
 * int is_prime_number - returns 1 if the input integer is a prime number
 * otherwise return 0
 * @n: the number 
 *
 * Return: if the input integer is a prime number return 1 
 * otherwise return 0
 */
int is_prime_number(int n)
{
    int i = 1;
	while (i < n)
    {
        int k = n / i;
        i++ ;
		return (int k);

	if (int k == 0)
	{	return (1);
    }
	else
    {   return (0);

    }
}
}
